rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /descuentos/{recordId} {
      // La consulta de empleados no tiene login en la interfaz actual.
      allow read: if true;
      allow create, update, delete: if request.auth != null;
    }
  }
}
